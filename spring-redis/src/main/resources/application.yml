
spring:
  redis:
    host: 120.24.242.44
    port: 60001
    password: xdp123456
    database: 0
    jedis:
      pool:
        max-active: 32
        max-idle: 4
        max-wait: 3000
        min-idle: 0

# 服务端口8080 服务上下文路径 /redis
server:
  servlet:
    context-path: /redis
  port: 8080